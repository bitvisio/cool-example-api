apiVersion: apps/v1
kind: Deployment
metadata:
  name: REPO_NAME
spec:
  replicas: 1
  selector:
    matchLabels:
      environment: prod
  template:
    metadata:
      labels:
        environment: prod
    spec:
      imagePullSecrets:
      - name: Cool-api-prod
      containers:
      - name: REPO_NAME
        env:
        - name: ENVIRONMENT
          value: "production"
